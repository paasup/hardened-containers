# Default build variant — used when no base_os input is given
# (images/<image>/<DEFAULT_BASE_OS>.build.env)
DEFAULT_BASE_OS=source

# Display group in the root README's published-images table.
# See scripts/build/render-published-images-table.py.
CATEGORY=APISIX

# Upstream support line — which release line this image is entitled to sit on.
# `manual` because endoflife.date has no entry for this project; SUPPORT_REF is what a
# person reads instead. See docs/image-authoring/support-policy.md.
SUPPORT_SOURCE=manual
SUPPORT_REF=https://github.com/apache/apisix-ingress-controller/releases
SUPPORT_NOTE="No maintenance branch exists — only master, as recorded in this image's README. Fixes land on master and reach users through the next tag, so only the newest tag is supported."
